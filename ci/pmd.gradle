plugins.apply("pmd")

pmd {
    toolVersion = '5.4.1'
}

task pmd(type: Pmd){
    ignoreFailures = false
    ruleSetFiles = files("${project.rootDir}/ci/pmd.xml")
    ruleSets = []

    source = fileTree('src/main/java')
}